name: Deploy NodeJS application to Hostinger
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        users: actions/checkout@v4

      - name: Deply via SSH
        users: appleboy/ssh-action@1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /root/gh-actions-test
            git pull
            docker compose up -d --build

